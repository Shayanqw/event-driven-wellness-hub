# wellness-resource-service/src/test/resources/application-test.properties

# Spring TestContainers will detect these settings and dynamically manage the containers
# This is the standard way to run containerized tests with Spring Boot 3
spring.test.database.replace=none
spring.datasource.url=jdbc:tc:postgresql:14-alpine:///testdb
spring.data.redis.host=redis